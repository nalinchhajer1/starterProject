// No state to register for pageone feature yet
export function registerPageOneFeature(
    injectReducer: (key: string, reducer: any, meta?: { persist?: any }) => void,
    injectSaga: (key: string, saga: any) => void
) {
    // Future: Register pageone state and sagas here when needed
}
