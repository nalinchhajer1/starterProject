import { StyleSheet } from 'react-native';

export const otherViewStyles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    },
    title: {
        fontSize: 18,
        marginBottom: 10
    },
    description: {
        fontSize: 14,
        color: '#666'
    }
});
