// RootState type definition without circular dependencies
export interface RootState {
    appState: any; // Will be properly typed when features are available
    viewState: any; // Will be properly typed when features are available
}
